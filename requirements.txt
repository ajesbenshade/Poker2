# Core dependencies for Poker2
# Note: install PyTorch separately for your accelerator before this file when possible.
# For AMD ROCm on the RX 7900 XT, use the current ROCm wheel command from:
# https://pytorch.org/get-started/locally/
# The plain `torch` entry below may install a CPU/CUDA wheel depending on platform and index.

torch
numpy
ray
eval7
psutil
tensorboard
scikit-learn
lmdb
